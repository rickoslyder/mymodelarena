# ArenaLab

The production ArenaLab implementation lives in [`arenalab/`](./arenalab/README.md). It provides a deterministic, replayable multi-agent Mafia environment, direct model-provider adapters, immutable event provenance, a public-safe projection, a browser replay console, adversarial validation, CI, and Vercel deployment configuration.
